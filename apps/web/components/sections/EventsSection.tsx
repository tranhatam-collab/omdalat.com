export function EventsSection() {
  return (
    <section className="runtime-section">
      <p className="runtime-kicker">Events</p>
      <h2>Upcoming moments that grow density</h2>
      <p>Event listing and detail routes can now hang off the runtime scaffold without changing the current static flow.</p>
    </section>
  );
}
