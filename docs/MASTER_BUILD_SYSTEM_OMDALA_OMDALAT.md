# MASTER_BUILD_SYSTEM_OMDALA_OMDALAT

Legacy note:
this mixed build file is no longer an active source of truth for the `omdalat.com` repository.

Use this file instead:

- `docs/MASTER_BUILD_SYSTEM_OMDALAT.md`

Reason:
the current repository has been locked to OMDALAT-only scope so DEV can build `omdalat.com` and `app.omdalat.com` without inheriting unrelated global-brand assumptions.
